# v7.6.0 GitHub Pages 部署

1. 將 `Liangshan_v7.6.0` 內容放在 GitHub 儲存庫根目錄。
2. 確認 `.github/workflows/pages.yml` 已上傳。
3. GitHub → Settings → Pages。
4. Source 選擇 **GitHub Actions**。
5. 推送到 `main`，等待 Actions 完成。
6. 開啟 Pages 網址，進入遊戲「雲端」頁執行營運診斷。
7. 依 `LIVE_DEPLOYMENT_CHECKLIST.md` 完成實機驗收。

所有資源採相對路徑，可部署於 `https://帳號.github.io/儲存庫/` 子路徑。
